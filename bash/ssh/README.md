# ssh bashrc setup

## Normal vs multiple sessions

A normal session can be started with y/Y. An "extra" session can be started with +.
When the normal session terminal is closed, all other sessions are also closed.

## SSH keys partial loading
Probably not all defined keys need to be loaded everytime. Just press enter without a password to skip a certain key. 
